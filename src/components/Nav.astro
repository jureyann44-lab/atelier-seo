<nav class="nav transparent">
  <div class="nav-container">
    <a href="/" class="nav-logo" aria-label="Retour à l'accueil Habeprod">
      <img
        src="/logo-habeprod-light.svg"
        alt="Habeprod"
        class="logo-site logo-light"
      />
      <img
        src="/logo-habeprod-dark.svg"
        alt="Habeprod"
        class="logo-site logo-dark"
      />
    </a>

    <ul class="nav-links">
      <li><a href="/#agence">L'agence</a></li>
      <li><a href="/#services">Nos services</a></li>
      <li><a href="/#expertise">Notre expertise</a></li>
      <li><a href="/#projects">Nos réalisations</a></li>
      <li><a href="/#contact">Contact</a></li>

      <li class="nav-social">
        <div class="social-links-mobile">
          <a
            href="https://www.instagram.com/habeprod/"
            target="_blank"
            rel="noopener noreferrer"
          >
            <img
              src="/logo-instagram.svg"
              class="icon-social"
              alt="Instagram"
            />
          </a>
          <a
            href="https://www.linkedin.com/company/95178904/"
            target="_blank"
            rel="noopener noreferrer"
          >
            <img src="/logo-linkedin.svg" class="icon-social" alt="LinkedIn" />
          </a>
          <a
            href="https://discord.com"
            target="_blank"
            rel="noopener noreferrer"
          >
            <img src="/logo-discord.svg" class="icon-social" alt="Discord" />
          </a>
        </div>
      </li>
    </ul>

    <div class="nav-right">
      <div class="social-links">
        <a
          href="https://www.instagram.com/habeprod/"
          target="_blank"
          rel="noopener noreferrer"
        >
          <img
            src="/logo-instagram.svg"
            alt="Instagram"
            class="icon-social icon-light"
          />
          <img
            src="/logo-instagram-dark.svg"
            alt="Instagram"
            class="icon-social icon-dark"
          />
        </a>
        <a
          href="https://www.linkedin.com/company/95178904/"
          target="_blank"
          rel="noopener noreferrer"
        >
          <img
            src="/logo-linkedin.svg"
            alt="LinkedIn"
            class="icon-social icon-light"
          />
          <img
            src="/logo-linkedin-dark.svg"
            alt="LinkedIn"
            class="icon-social icon-dark"
          />
        </a>
        <a href="https://discord.com" target="_blank" rel="noopener noreferrer">
          <img
            src="/logo-discord.svg"
            alt="Discord"
            class="icon-social icon-light"
          />
          <img
            src="/logo-discord-dark.svg"
            alt="Discord"
            class="icon-social icon-dark"
          />
        </a>
      </div>

      <button class="burger" aria-label="Ouvrir le menu" aria-expanded="false">
        <img
          src="/menu-burger-light.svg"
          alt=""
          class="burger-icon burger-icon-light"
        />
        <img
          src="/menu-burger-dark.svg"
          alt=""
          class="burger-icon burger-icon-dark"
        />
        <img
          src="/icon-close-light.svg"
          alt=""
          class="burger-close burger-close-light"
        />
        <img
          src="/icon-close-dark.svg"
          alt=""
          class="burger-close burger-close-dark"
        />
      </button>
    </div>
  </div>
</nav>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: transparent;
    border-bottom: 1px solid rgba(255, 255, 255, 0.25);
    transition: all var(--transition-speed) ease;
  }

  .nav.scrolled {
    background: rgba(255, 255, 255, 0.98);
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(14px);
  }

  .nav-container {
    max-width: var(--layout-width);
    margin: 0 auto;
    padding: 1.5rem var(--gutter);
    display: flex;
    align-items: center;
  }

  .nav-logo {
    position: relative;
    padding-right: 2.5rem;
    margin-right: 2.5rem;
    display: flex;
    align-items: center;
  }

  .nav-logo::after {
    content: "";
    position: absolute;
    top: 50%;
    right: 0;
    transform: translateY(-50%);
    width: 1px;
    height: 26px;
    background: rgba(255, 255, 255, 0.6);
    transition: background var(--transition-speed);
  }

  .logo-site {
    height: 32px;
    display: block;
  }

  .logo-light {
    display: block;
  }
  .logo-dark {
    display: none;
  }

  .nav-links {
    display: flex;
    gap: 2.5rem;
    list-style: none;
    font-size: 0.75rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .nav-links a {
    text-decoration: none;
    opacity: 0.8;
    transition: all var(--transition-speed);
  }

  .nav-links a:hover {
    opacity: 1;
    transform: translateY(-1px);
  }

  .nav.transparent .nav-links a {
    color: #ffffff;
  }

  .nav.scrolled .nav-links a {
    color: #0a0a0a;
    opacity: 0.92;
  }

  .nav.scrolled .nav-links a:hover {
    opacity: 1;
    transform: translateY(-1px);
  }

  .nav-right {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .social-links {
    display: flex;
    gap: 0.75rem;
  }

  .social-links a,
  .social-links-mobile a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 38px;
    height: 38px;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.45);
    transition: all var(--transition-speed);
  }

  .social-links a:hover,
  .social-links-mobile a:hover {
    box-shadow: 0 8px 30px rgba(255, 255, 255, 0.4);
    transform: translateY(-2px);
  }

  .nav.scrolled .social-links a,
  .nav.scrolled .social-links-mobile a {
    border-color: rgba(0, 0, 0, 0.35);
  }

  .nav.scrolled .social-links a:hover,
  .nav.scrolled .social-links-mobile a:hover {
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
  }

  .icon-social {
    width: 18px;
    height: 18px;
  }

  .icon-light {
    display: block;
  }
  .icon-dark {
    display: none;
  }

  .nav.scrolled .logo-light {
    display: none;
  }
  .nav.scrolled .logo-dark {
    display: block;
  }
  .nav.scrolled .icon-light {
    display: none;
  }
  .nav.scrolled .icon-dark {
    display: block;
  }
  .nav.scrolled .nav-logo::after {
    background: rgba(0, 0, 0, 0.25);
  }

  .burger {
    display: none;
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    transition: all var(--transition-speed);
  }

  .burger:hover {
    transform: scale(1.1);
  }

  .burger-icon {
    width: 28px;
    height: 28px;
    display: block;
  }

  .burger-icon-dark {
    display: none;
  }
  .nav.scrolled .burger-icon-light {
    display: none;
  }
  .nav.scrolled .burger-icon-dark {
    display: block;
  }

  .burger-close {
    width: 24px;
    height: 24px;
    display: none;
  }

  .burger-close-light,
  .burger-close-dark {
    display: none;
  }

  .nav.open .burger-icon {
    display: none;
  }

  .nav.open .burger-close-light {
    display: block;
  }

  .nav.open.scrolled .burger-close-light {
    display: none;
  }
  .nav.open.scrolled .burger-close-dark {
    display: block;
  }

  .nav-social {
    display: none;
  }

  /* 🖥️ DESKTOP - Overlay transparent (seulement non-tactile) */
  @media (min-width: 1101px) and (pointer: fine) {
    .nav.transparent {
      background: linear-gradient(
        to bottom,
        rgba(0, 0, 0, 0.75),
        rgba(0, 0, 0, 0.3)
      );
      backdrop-filter: blur(8px);
    }
  }

  /* 📱 MOBILE LANDSCAPE - Alignement Nav à 90% comme les autres blocs */
  @media (min-width: 667px) and (max-width: 960px) and (orientation: landscape) and (max-height: 450px) {
    .nav-container {
      max-width: 90%;
      margin: 0 auto;
      padding: 1rem 0;
    }

    .logo-site {
      height: 28px;
    }

    .burger-icon {
      width: 24px;
      height: 24px;
    }
  }

  /* 📱 iPad Mini PORTRAIT - Alignement Nav à 90% */
  @media (min-width: 768px) and (max-width: 820px) and (orientation: portrait) {
    .nav-container {
      max-width: 90%;
      margin: 0 auto;
    }
  }

  /* 🔥 MOBILE (tactile OU petit écran) - Burger menu */
  @media (max-width: 1100px), (max-width: 1023px) and (pointer: coarse) {
    .nav-container {
      padding: 1.5rem var(--gutter);
    }

    /* Cache les liens du menu classique */
    .nav-links {
      position: fixed;
      inset: 70px var(--gutter) auto var(--gutter);
      flex-direction: column;
      background: rgba(0, 0, 0, 0.95);
      padding: 2rem 1.5rem 2.5rem;
      border-radius: 16px;
      border: 1px solid rgba(255, 255, 255, 0.15);
      display: none;
      gap: 1rem;
    }

    .nav.open .nav-links {
      display: flex;
    }

    .nav.open .nav-links a {
      color: #ffffff;
      opacity: 1;
      font-size: 0.85rem;
    }

    /* Cache les icônes sociales desktop */
    .nav-right .social-links {
      display: none;
    }

    /* Affiche les icônes sociales dans le menu burger */
    .nav-social {
      display: block;
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid rgba(255, 255, 255, 0.3);
    }

    .social-links-mobile {
      display: flex;
      gap: 1rem;
      justify-content: center;
    }

    /* Affiche le burger */
    .burger {
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* Supprime le séparateur du logo en tablette/mobile */
    .nav-logo {
      padding-right: 0;
      margin-right: 0;
    }

    .nav-logo::after {
      display: none;
    }
  }
</style>

<script>
  const nav = document.querySelector(".nav");
  const burger = document.querySelector(".burger");
  const links = document.querySelector(".nav-links");

  function handleScroll() {
    if (!nav) return;
    if (window.scrollY > 50) {
      nav.classList.add("scrolled");
      nav.classList.remove("transparent");
    } else {
      nav.classList.remove("scrolled");
      nav.classList.add("transparent");
    }
  }

  function toggleMenu() {
    const isOpen = nav?.classList.toggle("open");
    if (burger) {
      burger.setAttribute(
        "aria-label",
        isOpen ? "Fermer le menu" : "Ouvrir le menu",
      );
      burger.setAttribute("aria-expanded", isOpen ? "true" : "false");
    }
  }

  function closeMenu() {
    if (!nav) return;
    nav.classList.remove("open");
    if (burger) {
      burger.setAttribute("aria-label", "Ouvrir le menu");
      burger.setAttribute("aria-expanded", "false");
    }
  }

  window.addEventListener("scroll", handleScroll, { passive: true });
  burger?.addEventListener("click", toggleMenu);
  links
    ?.querySelectorAll("a")
    .forEach((link) => link.addEventListener("click", closeMenu));
  handleScroll();
</script>
