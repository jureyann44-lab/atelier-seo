<section class="hero">
  <img
    src="/background-hero.webp"
    alt=""
    class="hero-bg"
    loading="eager"
    fetchpriority="high"
    decoding="async"
    width="1920"
    height="1080"
  />

  <div class="hero-inner">
    <div class="hero-copy">
      <h1 class="visually-hidden">Habeprod agence de communication le Mans</h1>

      <div class="hero-kicker">HABEPROD</div>

      <h2 class="hero-title">Stratégie digitale & visibilité au Mans</h2>

      <p class="hero-subtitle">
        Nous concevons des dispositifs digitaux cohérents et performants pour
        structurer et amplifier votre visibilité locale.
      </p>

      <a href="#contact" class="hero-cta">
        <span>Demander un diagnostic</span>
      </a>
    </div>

    <div class="hero-visual"></div>
  </div>

  <div class="hero-bottom-strip"></div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    display: flex;
    align-items: center;
    padding: 7rem 0 6rem;
    background: #010712;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    z-index: 0;
    opacity: 1;
  }

  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
      radial-gradient(
        circle 600px at 10% 20%,
        rgba(1, 7, 18, 0.95) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle 600px at 90% 20%,
        rgba(1, 7, 18, 0.95) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle 500px at 15% 90%,
        rgba(1, 7, 18, 0.95) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle 500px at 85% 90%,
        rgba(1, 7, 18, 0.95) 0%,
        transparent 40%
      );
    z-index: 1;
  }

  .hero::after {
    content: "";
    position: absolute;
    inset: 0;
    background:
      radial-gradient(
        circle 500px at 10% 10%,
        rgba(1, 7, 18, 0.92) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle 500px at 90% 10%,
        rgba(1, 7, 18, 0.92) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle 600px at 15% 90%,
        rgba(1, 7, 18, 0.88) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle 600px at 85% 90%,
        rgba(1, 7, 18, 0.88) 0%,
        transparent 40%
      ),
      linear-gradient(
        90deg,
        rgba(1, 7, 18, 0.65) 0%,
        rgba(1, 7, 18, 0.35) 35%,
        rgba(1, 7, 18, 0.15) 65%,
        rgba(1, 7, 18, 0.05) 100%
      );
    z-index: 1;
  }

  .hero-inner {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.1fr);
    align-items: center;
    column-gap: 4rem;
    max-width: var(--layout-width);
    margin: 0 auto;
    padding-left: var(--gutter);
    padding-right: var(--gutter);
  }

  .hero-copy {
    color: #ffffff;
  }

  .hero-kicker {
    font-size: 30px;
    font-weight: 400;
    letter-spacing: -0.5px;
    position: relative;
    display: inline-block;
    margin-bottom: 1rem;
  }

  .hero-kicker::after {
    content: "°";
    color: #eb6432;
    margin-left: 3px;
  }

  .hero-title {
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    font-size: 1.1rem;
    margin-bottom: 2.5rem;
    max-width: 520px;
    opacity: 0.9;
  }

  .hero-cta {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.95rem 2.6rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.35);
    text-decoration: none;
    font-size: 0.85rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #ffffff;
    background: transparent;
    transition: all var(--transition-speed);
  }

  .hero-cta:hover {
    box-shadow: 0 6px 25px rgba(255, 255, 255, 0.25);
    transform: translateY(-1px);
  }

  .hero-cta > span {
    position: relative;
    z-index: 2;
  }

  .hero-bottom-strip {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 80%;
    height: 70px;
    background: #ffffff;
    z-index: 3;
  }

  .hero-visual {
    position: relative;
    height: 520px;
  }

  /* 📱 MOBILE LANDSCAPE - Typo ultra compacte */
  @media (max-width: 767px) and (orientation: landscape) {
    .hero {
      min-height: 100vh;
      padding: 4rem 0 2rem;
    }

    .hero-inner {
      align-items: flex-start;
      padding-top: 4.5rem;
      padding-left: 24px;
      padding-right: 24px;
    }

    .hero-copy {
      padding-left: 0;
    }

    .hero-kicker {
      font-size: 18px;
      margin-bottom: 0.5rem;
    }

    .hero-title {
      font-size: 22px !important;
      margin-bottom: 0.75rem !important;
      line-height: 1.1 !important;
    }

    .hero-subtitle {
      font-size: 0.85rem;
      margin-bottom: 1rem;
      line-height: 1.4;
    }

    .hero-cta {
      padding: 0.65rem 1.75rem;
      font-size: 0.7rem;
    }

    .hero-bottom-strip {
      display: none;
    }
  }

  /* 📱 TABLETTE PORTRAIT + MOBILE */
  @media (max-width: 900px) {
    .hero {
      padding: 7rem 0 4.5rem;
    }

    .hero::before {
      display: none;
    }

    .hero::after {
      background: rgba(0, 0, 0, 0.4);
    }

    .hero-inner {
      grid-template-columns: 1fr;
      row-gap: 3rem;
    }

    .hero-visual {
      display: none;
    }

    .hero-bottom-strip {
      display: none;
    }
  }

  /* 📱 TABLETTE PORTRAIT - iPad Pro inclus */
  @media (min-width: 768px) and (max-width: 1024px) and (orientation: portrait) {
    .hero {
      align-items: center !important;
      padding-top: 7rem !important;
    }

    .hero-inner {
      margin: 0 !important;
      padding-left: var(--gutter) !important;
      padding-right: var(--gutter) !important;
      text-align: left !important;
      justify-items: start !important;
      align-items: center !important;
    }

    .hero-copy {
      text-align: left !important;
    }

    .hero-kicker,
    .hero-title,
    .hero-subtitle {
      text-align: left !important;
    }

    .hero-cta {
      margin-left: 0 !important;
      margin-right: auto !important;
    }
  }

  /* 💻 TABLETTE LANDSCAPE */
  @media (min-width: 901px) and (max-width: 1100px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
      justify-items: center;
    }

    .hero-subtitle {
      max-width: 600px;
    }

    .hero-visual {
      display: none;
    }

    .hero-bottom-strip {
      height: 60px;
    }
  }
</style>

<script>
  const preloadLink = document.createElement("link");
  preloadLink.rel = "preload";
  preloadLink.as = "image";
  preloadLink.href = "/background-hero.webp";
  preloadLink.fetchpriority = "high";
  document.head.appendChild(preloadLink);
</script>
