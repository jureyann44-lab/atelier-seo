<section class="hero">
  <div class="hero-noise"></div>

  <div class="hero-container">
    <div class="hero-content">

      <!-- ✅ Fix #1 & #9 : H2 pour le logo, H1 pour le mot-clé principal -->
      <div class="hero-title">
        <span class="hero-title-main">L'ATEL<span class="hero-i-wrap"><span class="hero-i">I</span><span class="hero-badge-above">
          <!-- ✅ Fix #8 : aria-hidden sur le SVG décoratif -->
          <svg class="badge-ring" viewBox="0 0 120 120" aria-hidden="true">
            <defs>
              <path id="circle-text" d="M 60,60 m -40,0 a 40,40 0 1,1 80,0 a 40,40 0 1,1 -80,0" />
            </defs>
            <text class="badge-text">
              <textPath href="#circle-text" startOffset="0%">SEO · LE MANS · SARTHE · LOCAL · </textPath>
            </text>
          </svg>
        </span></span>ER SEO</span>
      </div>

      <!-- ✅ Fix #1 & #9 : H1 avec le vrai mot-clé principal -->
      <h1 class="hero-h1">Consultant SEO au Mans</h1>

      <!-- ✅ Fix #2 : subtitle enrichi avec intention business -->
      <p class="hero-subtitle">
        Référencement Google, SEO local et stratégie de contenu pour les entreprises du Mans et de la Sarthe souhaitant améliorer leur visibilité et attirer plus de clients.
      </p>

      <!-- ✅ Fix #6 : micro élément SEO avec mots-clés -->
      <p class="hero-keywords">
        SEO local · Référencement Google · Consultant SEO Le Mans
      </p>

      <div class="hero-actions">
        <!-- ✅ Fix #4 : CTA avec "gratuit" + preuve simplifiée -->
        <a href="/audit-seo" class="btn-hero-primary">
          Demander un audit SEO gratuit
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="16" height="16" aria-hidden="true">
            <path fill-rule="evenodd" d="M3 10a.75.75 0 01.75-.75h10.638L10.23 5.29a.75.75 0 111.04-1.08l5.5 5.25a.75.75 0 010 1.08l-5.5 5.25a.75.75 0 11-1.04-1.08l4.158-3.96H3.75A.75.75 0 013 10z" clip-rule="evenodd" />
          </svg>
        </a>
        <p class="hero-proof">✓ Réponse sous 48h</p>
      </div>

    </div>

  </div>

  <div class="hero-bottom">
    <span class="hero-scroll-label">Défiler</span>
    <div class="hero-scroll-line"></div>
  </div>
</section>

<style is:global>
  @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap');
</style>

<style>

  .hero {
    position: relative;
    min-height: 100vh;
    background: #080808;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    padding-top: 80px;
  }

  /* Grain texture */
  .hero-noise {
    position: absolute;
    inset: 0;
    z-index: 1;
    opacity: 0.04;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
    background-size: 200px 200px;
    pointer-events: none;
  }

  /* Radial glow bottom-left */
  .hero::before {
    content: "";
    position: absolute;
    bottom: -10%;
    left: -5%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(255,255,255,0.04) 0%, transparent 70%);
    z-index: 1;
    pointer-events: none;
  }

  .hero-container {
    position: relative;
    z-index: 2;
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: var(--layout-width);
    margin: 0 auto;
    padding: 6rem var(--gutter) 4rem;
    width: 100%;
  }

  .hero-content {
    flex: 1;
    max-width: 720px;
  }

  /* Big title (now a div, not h1) */
  .hero-title {
    margin: 0 !important;
    padding: 6rem 0 0.3rem !important;
    line-height: 0.85 !important;
    font-size: unset !important;
    font-weight: unset !important;
    letter-spacing: unset !important;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.25s;
  }

  .hero-title-main {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 8rem;
    color: #ffffff;
    letter-spacing: 0.02em;
    display: block;
  }

  .hero-title-main sup {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.25em;
    font-weight: 300;
    vertical-align: super;
    opacity: 0.5;
    letter-spacing: 0;
  }

  /* ✅ Fix #1 : H1 vrai (mot-clé principal) */
  .hero-h1 {
    font-family: 'DM Sans', sans-serif;
    font-size: clamp(1rem, 2vw, 1.25rem);
    font-weight: 400;
    color: rgba(255, 255, 255, 0.45);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin: 0.15rem 0 2rem;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.4s;
  }

  /* Subtitle */
  .hero-subtitle {
    font-family: 'DM Sans', sans-serif;
    font-size: clamp(1rem, 1.5vw, 1.15rem);
    font-weight: 400;
    color: #ffffff;
    line-height: 1.55;
    margin: 0 0 1.25rem;
    max-width: 560px;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.55s;
  }

  /* ✅ Fix #7 : font-size simplifié à 1rem */
  .hero-body {
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem;
    font-weight: 300;
    font-style: italic;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.7;
    margin: 0 0 1.25rem;
    max-width: 520px;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.7s;
  }

  /* ✅ Fix #6 : style pour les mots-clés SEO */
  .hero-keywords {
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem;
    font-weight: 400;
    color: #ffffff;
    letter-spacing: 0.08em;
    margin: 0 0 2.5rem;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.78s;
  }

  .hero-proof {
    margin: 0.85rem 0 0;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.78rem;
    font-weight: 400;
    color: #ffffff;
    letter-spacing: 0.04em;
  }

  /* CTA */
  .hero-actions {
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.85s;
  }

  .btn-hero-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: transparent;
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.6);
    font-family: 'DM Sans', sans-serif;
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    padding: 1rem 1.75rem;
    border-radius: 6px;
    transition: background 0.25s ease, border-color 0.25s ease, transform 0.25s ease, gap 0.25s ease;
  }

  .btn-hero-primary:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: #ffffff;
    transform: translateY(-2px);
    gap: 0.9rem;
  }

  /* Badge au-dessus du I */
  .hero-i-wrap {
    position: relative;
    display: inline-block;
  }

  .hero-i {
    display: inline-block;
  }

  .hero-badge-above {
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    width: 90px;
    height: 90px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 0.1em;
  }

  .badge-ring {
    position: absolute;
    inset: 0;
    animation: spin 16s linear infinite;
  }

  .badge-text {
    font-family: 'DM Sans', sans-serif;
    font-size: 10.5px;
    fill: rgba(255, 255, 255, 0.4);
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .badge-center {
    font-size: 1.5rem;
    color: rgba(255, 255, 255, 0.7);
    z-index: 1;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  /* Bottom scroll indicator */
  .hero-bottom {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    padding-bottom: 2.5rem;
    opacity: 0;
    animation: fadeIn 1s ease forwards 1.2s;
  }

  .hero-scroll-label {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.3);
  }

  .hero-scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, rgba(255,255,255,0.3), transparent);
    animation: scrollLine 2s ease-in-out infinite;
  }

  @keyframes scrollLine {
    0%, 100% { transform: scaleY(1); opacity: 1; }
    50% { transform: scaleY(0.5); opacity: 0.4; }
  }

  /* Animations */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-container {
      padding: 4rem 1.5rem 3rem;
    }

    .hero-title-main {
      font-size: 5rem;
    }

    .hero-badge {
      display: none;
    }
  }
</style>

<script>
  // ✅ Fix #5 : sélecteur corrigé (.hero-badge-above au lieu de .hero-badge)
  const badge = document.querySelector('.hero-badge-above');
  badge?.addEventListener('click', (e) => {
    e.preventDefault();
    const target = document.querySelector('#services');
    if (!target) return;

    const overlay = document.createElement('div');
    overlay.style.cssText = `
      position: fixed;
      inset: 0;
      background: #080808;
      z-index: 9999;
      transform: translateX(100%);
      transition: transform 0.5s cubic-bezier(0.76, 0, 0.24, 1);
    `;
    document.body.appendChild(overlay);

    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        overlay.style.transform = 'translateX(0)';
      });
    });

    setTimeout(() => {
      target.scrollIntoView({ block: 'start' });
      overlay.style.transition = 'transform 0.5s cubic-bezier(0.76, 0, 0.24, 1) 0.1s';
      overlay.style.transform = 'translateX(-100%)';
      setTimeout(() => overlay.remove(), 700);
    }, 520);
  });
</script>